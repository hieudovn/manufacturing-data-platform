# Local Next.js development.
# Use http://localhost:8000 to call the FastAPI backend directly.
# Leave empty in production so the frontend calls same-origin /api through Caddy.
NEXT_PUBLIC_API_URL=http://localhost:8000
